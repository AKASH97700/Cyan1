user:
  name: ishikki
  github: ishikki-akabane

language: "python"
python_version: "3.13"

commands:
  build:
    - "pip install -r requirements.txt"
  upgrade_package:
    - "nano inxi"
  start:
    - "python3 -m Mikobot"

env:
  - "ENVIRONMENT=production"
