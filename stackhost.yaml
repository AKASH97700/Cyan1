user:
  name: akash 
  github: AKASH97700

language: "python"
python_version: "3.12"

commands:
  build:
    - "pip install -r requirements.txt"
  package:
    - "nano inxi"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
